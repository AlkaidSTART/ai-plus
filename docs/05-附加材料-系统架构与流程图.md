# InsightX · 系统架构与业务流程图全景

本文档包含 **InsightX · 全球跨境电商 AI 市场洞察与动态决策系统** 的核心技术架构、Agent 状态机编排、数据处理流、前端功能站点导航及双模式部署拓扑图。所有图表均基于 Mermaid 标准语法构建。

---

## 1. 系统整体架构图（分层架构）

**架构说明**：系统采用现代化前后端分离与微服务架构。前端基于 Next.js 16 + shadcn/ui + ECharts 提供交互分析与数据可视化看板；BFF 层通过 Next.js API Routes 承担鉴权、聚合与 SSE 流式事件透传；AI 核心服务采用 Python FastAPI + LangGraph 实现多 Agent 状态机调度；底层依托 PostgreSQL + pgvector 提供结构化与向量混合检索，Redis 支撑任务队列与高频缓存，MinIO/S3 存储海量买家实拍多模态证据。

```mermaid
flowchart TD
    %% 样式定义
    classDef userLayer fill:#f8fafc,stroke:#64748b,stroke-width:2px,color:#0f172a;
    classDef feLayer fill:#e0f2fe,stroke:#0284c7,stroke-width:2px,color:#0369a1;
    classDef bffLayer fill:#f0fdf4,stroke:#16a34a,stroke-width:2px,color:#15803d;
    classDef aiLayer fill:#fef3c7,stroke:#d97706,stroke-width:2px,color:#b45309;
    classDef modelLayer fill:#fce7f3,stroke:#db2777,stroke-width:2px,color:#be185d;
    classDef dataLayer fill:#f3e8ff,stroke:#9333ea,stroke-width:2px,color:#7e22ce;

    %% 1. 用户层
    subgraph Layer_User ["1. 用户终端层"]
        U1["出海工贸一体企业"]
        U2["跨境品牌卖家"]
        U3["产品研发与供应链团队"]
    end
    class Layer_User,U1,U2,U3 userLayer;

    %% 2. 前端层
    subgraph Layer_FE ["2. 前端展示层 (Next.js 16 App Router)"]
        FE_Dash["实时数据大屏 (ECharts)"]
        FE_Comp["跨平台竞品全景看板"]
        FE_VOC["多模态 VOC 缺陷探查"]
        FE_Decision["双栏工程改款决策台"]
        FE_Risk["现金流风控与熔断模拟器"]
    end
    class Layer_FE,FE_Dash,FE_Comp,FE_VOC,FE_Decision,FE_Risk feLayer;

    %% 3. BFF层
    subgraph Layer_BFF ["3. BFF 服务聚合层 (Next.js API Routes)"]
        BFF_Auth["身份认证 & 权限网关 (JWT)"]
        BFF_SSE["SSE 流式输出网关 (Agent实时流)"]
        BFF_Agg["GraphQL/REST 业务聚合适配器"]
    end
    class Layer_BFF,BFF_Auth,BFF_SSE,BFF_Agg bffLayer;

    %% 4. AI核心引擎层
    subgraph Layer_AI ["4. AI 核心计算层 (FastAPI + LangGraph)"]
        AI_Orch["LangGraph 智能决策状态机"]
        AI_Scraper["多源采集调度 (Playwright + httpx)"]
        AI_Embed["向量特征提取引擎 (bge-m3)"]
        AI_VLM["视觉取证与缺陷定损引擎"]
        AI_Finance["逆向财务与供应链精算器"]
        AI_Trace["全链路证据追溯与回测引擎"]
    end
    class Layer_AI,AI_Orch,AI_Scraper,AI_Embed,AI_VLM,AI_Finance,AI_Trace aiLayer;

    %% 5. 模型底座层
    subgraph Layer_Model ["5. 大模型与算法底座"]
        M_Sonnet["Claude Sonnet 4 (主力语义与分析)"]
        M_Opus["Claude Opus 4 (复杂权衡与最终决策)"]
        M_Vision["Claude Vision (多模态实拍图破损取证)"]
        M_BGE["bge-m3 (1024维 多语言跨平台对齐)"]
    end
    class Layer_Model,M_Sonnet,M_Opus,M_Vision,M_BGE modelLayer;

    %% 6. 数据存储层
    subgraph Layer_Data ["6. 数据持久化与基础设施层"]
        DB_PG["PostgreSQL + pgvector (业务数据 + 向量索引)"]
        DB_Redis["Redis (Pub/Sub + Celery 异步队列 + 缓存)"]
        DB_S3["MinIO / AWS S3 (买家差评图/产品图多模态仓)"]
    end
    class Layer_Data,DB_PG,DB_Redis,DB_S3 dataLayer;

    %% 链路连接与协议标注
    U1 & U2 & U3 -->|HTTPS / WSS| Layer_FE
    Layer_FE -->|REST API 请求| BFF_Agg
    Layer_FE -.->|SSE 长连接事件流| BFF_SSE
    
    BFF_Agg -->|REST / gRPC| AI_Orch
    BFF_SSE -.->|Async Event Stream| AI_Orch
    
    AI_Orch --> AI_Embed & AI_VLM & AI_Finance & AI_Trace
    AI_Scraper -->|异步任务分发| DB_Redis
    
    AI_Embed -->|调用 Embedding| M_BGE
    AI_VLM -->|调用 VLM 分析| M_Vision
    AI_Orch -->|意图识别 & 初审| M_Sonnet
    AI_Orch -->|仲裁 & 深度决策| M_Opus

    AI_Orch <-->|读写商品与向量| DB_PG
    AI_Orch <-->|任务状态与流缓存| DB_Redis
    AI_VLM <-->|读写差评图像证据| DB_S3
```

---

## 2. LangGraph Agent 决策工作流图（状态机）

**流程说明**：系统以 7 步闭环 Agent 状态机为核心。从竞品数据接入开始，经过多语言对齐与多模态实拍图取证，生成“产品本体+包装履约”双栏改款方案；随后进入“成本约束与现金流否决”校验节点，若财务 ROI 或现金流安全垫不达标，触发**条件回退边**，将约束参数打回改款引擎降级重算；通过后进行全链路证据绑定与历史解释力回测，最终输出高置信度立项决策报告。

```mermaid
stateDiagram-v2
    direction TB

    [*] --> Node1_Ingestion: 触发选品/改款任务

    state "1. 多源竞品数据接入节点" as Node1_Ingestion {
        direction LR
        In1: 目标品类/关键词/ASIN
        Out1: 原始商品元数据+评论+图片
        In1 --> Out1
    }

    state "2. 多语言语义归一与向量对齐节点" as Node2_Alignment {
        direction LR
        In2: 跨平台多语言文本
        Out2: bge-m3 1024维语义向量+标准实体库
        In2 --> Out2
    }

    state "3. 多模态视觉差评取证节点 (VLM)" as Node3_VLM {
        direction LR
        In3: 买家实拍破损图 + 差评上下文
        Out3: 结构断裂/材质劣化等物理缺陷特征集
        In3 --> Out3
    }

    state "4. 双栏改款决策引擎" as Node4_Decision {
        direction LR
        In4: 物理缺陷集 + 市场诉求 + 成本容差
        Out4: 产品物理改款清单 + 包装尺寸降级方案
        In4 --> Out4
    }

    state "5. 成本约束与现金流动态否决" as Node5_FinancialGate {
        direction LR
        In5: 改款方案 + 模具成本 + MOQ + FBA阶梯费率
        Out5: 财务测算模型 (ROI/毛利率/回本周期)
        In5 --> Out5
    }

    state "6. 全链路证据链溯源节点" as Node6_EvidenceTrace {
        direction LR
        In6: 决策方案 + 原始评论ID + 图像坐标 + 竞品ASIN
        Out6: 可穿透证据链拓扑图
        In6 --> Out6
    }

    state "7. 历史解释力回测与输出" as Node7_Backtest {
        direction LR
        In7: 决策方案 + 历史时序销量/价格波动数据
        Out7: 最终综合立项建议书 (JSON/PDF)
        In7 --> Out7
    }

    Node1_Ingestion --> Node2_Alignment: 抓取完成 (Raw Data)
    Node2_Alignment --> Node3_VLM: 向量空间构建完成
    Node3_VLM --> Node4_Decision: 结构化物理缺陷归因输出
    Node4_Decision --> Node5_FinancialGate: 双栏方案送审

    %% 条件分支与回退
    state Check_Gate <<choice>>
    Node5_FinancialGate --> Check_Gate: 评估毛利与资金风险
    
    Check_Gate --> Node4_Decision: ❌ 触发财务否决 (ROI不达标/超资金限额) -> 附带约束参数打回重算
    Check_Gate --> Node6_EvidenceTrace:  财务风控通过 (满足安全垫要求)

    Node6_EvidenceTrace --> Node7_Backtest: 证据链锚定完成
    Node7_Backtest --> [*]: 决策报告生成并推送到前端
```

---

## 3. 全生命周期数据流图

**数据流说明**：展示数据从外部电商平台采集、中间清洗向量化、分层存储、混合检索、多模型协同分析，到最终结构化决策产出的端到端路径，明确标注各阶段的**数据格式**与**存储介质**。

```mermaid
flowchart LR
    %% 外部数据源
    subgraph DS ["外部数据源 (Multi-Source)"]
        DS_AMZ["Amazon (Listing/Reviews/Images)"]
        DS_TK["TikTok Shop (短视频反馈/带货SKU)"]
        DS_TEMU["Temu (价格带/买家秀)"]
    end

    %% 阶段1：采集与预处理
    subgraph P1 ["阶段 1: 采集与解析"]
        C_Worker["Playwright / httpx 爬虫集群"]
        C_Raw["原始文本 HTML / JSON / JPG 图像"]
    end

    %% 阶段2：向量化与存储
    subgraph P2 ["阶段 2: 归一化与存储"]
        V_Embed["bge-m3 语义向量化"]
        V_OSS["图像流落盘 (MinIO/S3)"]
        V_PG["PostgreSQL (结构化数据)"]
        V_Vec["pgvector (1024维向量表)"]
    end

    %% 阶段3：混合检索与组装
    subgraph P3 ["阶段 3: 混合检索与证据组装"]
        R_Hybrid["Hybrid Search (BM25 + 向量近似)"]
        R_Context["结构化上下文 (JSON Context Window)"]
    end

    %% 阶段4：AI 多模态推理
    subgraph P4 ["阶段 4: 模型推理与风控校验"]
        M_VLM_Run["Claude Vision 图像缺陷定损"]
        M_LLM_Run["Claude Sonnet/Opus 逻辑决策"]
        M_Sim_Run["Python 财务与 FBA 尺寸精算"]
    end

    %% 阶段5：终端呈现
    subgraph P5 ["阶段 5: 业务决策输出"]
        OUT_Report["双栏改款清单 (JSON Schema)"]
        OUT_Stream["SSE 实时日志与图表渲染"]
    end

    %% 数据流连接
    DS_AMZ & DS_TK & DS_TEMU -->|HTTP GET/API| C_Worker
    C_Worker -->|格式化提取| C_Raw
    
    C_Raw -->|文本流 (UTF-8)| V_Embed
    C_Raw -->|二进制图片 (Base64/Binary)| V_OSS
    C_Raw -->|结构化元数据 (JSONB)| V_PG
    
    V_Embed -->|Float Array| V_Vec
    
    V_PG & V_Vec -->|SQL + Vector Cosine Query| R_Hybrid
    V_OSS -->|Signed URL / Image Bytes| R_Hybrid
    
    R_Hybrid -->|RAG 上下文载荷| R_Context
    
    R_Context -->|提示词 + 图像 URL| M_VLM_Run
    R_Context -->|提示词 + 评论聚类| M_LLM_Run
    M_LLM_Run -->|参数化工程方案| M_Sim_Run
    
    M_Sim_Run -->|核验通过| OUT_Report
    M_Sim_Run -.->|重算反馈| M_LLM_Run
    OUT_Report -->|SSE Event Stream| OUT_Stream
```

---

## 4. 前端页面导航与信息架构图（站点地图）

**导航说明**：前端基于 Next.js 16 App Router 构建，提供从大盘概览、细分品类穿透、图像深度取证、决策生成到风控审计的一站式操作链路。

```mermaid
graph TD
    classDef mainNode fill:#1e293b,stroke:#0f172a,stroke-width:2px,color:#fff;
    classDef subNode fill:#f1f5f9,stroke:#94a3b8,stroke-width:1px,color:#0f172a;
    classDef leafNode fill:#e2e8f0,stroke:#cbd5e1,stroke-width:1px,color:#334155;

    Root["InsightX 平台导航根目录 (App Router)"]:::mainNode

    %% 一级导航
    Nav_Dash["📊 Dashboard (市场全景总览)"]:::subNode
    Nav_Comp["🔍 跨平台竞品分析 (/competitors)"]:::subNode
    Nav_VOC["👁️ 多模态评论洞察 (/voc-insights)"]:::subNode
    Nav_Decision["🛠️ 双栏改款决策台 (/product-redesign)"]:::subNode
    Nav_Risk["🛡️ 现金流风控与否决 (/risk-control)"]:::subNode
    Nav_Settings["⚙️ 系统与配置管理 (/settings)"]:::subNode

    Root --> Nav_Dash
    Root --> Nav_Comp
    Root --> Nav_VOC
    Root --> Nav_Decision
    Root --> Nav_Risk
    Root --> Nav_Settings

    %% Dashboard 子页面/模块
    Nav_Dash --> D1["全景宏观指标监控 (GMV/价格带分布)"]:::leafNode
    Nav_Dash --> D2["类目红海/蓝海机会雷达图"]:::leafNode
    Nav_Dash --> D3["进行中决策任务流状态追踪"]:::leafNode

    %% 竞品分析子页面
    Nav_Comp --> C1["跨平台 SKU 动态关联映射表"]:::leafNode
    Nav_Comp --> C2["价格波动与排名时序对齐曲线"]:::leafNode
    Nav_Comp --> C3["新晋黑马竞品穿透透视卡"]:::leafNode

    %% 多模态 VOC 子页面
    Nav_VOC --> V1["多语言差评语义聚类分析"]:::leafNode
    Nav_VOC --> V2["买家实拍破损/缺陷标注取证看板 (VLM)"]:::leafNode
    Nav_VOC --> V3["核心负向体验根因追溯链"]:::leafNode

    %% 双栏改款决策台
    Nav_Decision --> R1["栏一：产品本体物理改款清单 (模具/材料)"]:::leafNode
    Nav_Decision --> R2["栏二：包装与履约优化建议 (FBA尺寸降级)"]:::leafNode
    Nav_Decision --> R3["全链路证据链穿透查看器 (Evidence Trace)"]:::leafNode

    %% 风控与否决
    Nav_Risk --> K1["模具开模 + 生产 MOQ 成本精算表"]:::leafNode
    Nav_Risk --> K2["动态 ROI 门槛与现金流断裂熔断器"]:::leafNode
    Nav_Risk --> K3["历史解释力回测模拟验证沙盒"]:::leafNode

    %% 设置模块
    Nav_Settings --> S1["平台数据源 API & 爬虫代理配置"]:::leafNode
    Nav_Settings --> S2["LLM 决策模型与温度参数调优"]:::leafNode
    Nav_Settings --> S3["企业供应链基准成本/汇率库管理"]:::leafNode
```

---

## 5. 部署架构图（本地开发 vs 生产环境双模式）

**部署说明**：InsightX 支持双轨部署模式。本地及私有化环境采用 **Docker Compose** 容器化编排，一键拉起全栈微服务与依赖库；公有云生产环境采用 **Vercel (前端+BFF) + Railway (AI 核心引擎+数据库集群)** 的弹性 Serverless/Container 混合架构，兼顾高可用扩展与极致开发体验。

```mermaid
flowchart TB
    %% 样式
    classDef devBox fill:#eff6ff,stroke:#3b82f6,stroke-width:2px,color:#1d4ed8;
    classDef prodBox fill:#f0fdf4,stroke:#22c55e,stroke-width:2px,color:#15803d;
    classDef compNode fill:#ffffff,stroke:#64748b,stroke-width:1px,color:#0f172a;

    %% 本地开发模式
    subgraph Mode_Local ["模式 A: 本地与专有云环境 (Docker Compose 编排)"]
        direction TB
        subgraph DC_Network ["Docker Virtual Bridge Network (insightx-net)"]
            DC_FE["insightx-web:3000\n(Next.js App)"]:::compNode
            DC_AI["insightx-ai-core:8000\n(FastAPI + LangGraph)"]:::compNode
            DC_PG["insightx-postgres:5432\n(PostgreSQL + pgvector)"]:::compNode
            DC_Redis["insightx-redis:6379\n(Redis 8 Alpine)"]:::compNode
            DC_Minio["insightx-minio:9000\n(MinIO S3 Compatible)"]:::compNode
        end
        
        Client_Dev["本地开发者浏览器 (localhost:3000)"] --> DC_FE
        DC_FE -->|Internal Network| DC_AI
        DC_AI -->|SQL/Vector| DC_PG
        DC_AI -->|PubSub/Queue| DC_Redis
        DC_AI -->|S3 API| DC_Minio
    end
    class Mode_Local devBox;

    %% 生产云端模式
    subgraph Mode_Prod ["模式 B: 生产高可用环境 (Vercel Edge + Railway Cloud)"]
        direction TB
        
        subgraph Cloud_Edge ["1. Vercel Global Edge Network"]
            V_Web["Next.js 16 Frontend (SSR / Static Assets)"]:::compNode
            V_BFF["API Routes (Edge Runtime / SSE Proxy)"]:::compNode
            V_Web <--> V_BFF
        end

        subgraph Cloud_Railway ["2. Railway Cloud PaaS Cluster"]
            R_AI["FastAPI AI Engine (Auto-scaling Pods)"]:::compNode
            R_Worker["Celery Scraper Workers (Playwright Pool)"]:::compNode
            
            subgraph Cloud_DB ["Managed Data Cluster"]
                R_PG[("PostgreSQL + pgvector\n(Persistent Storage)")]:::compNode
                R_Redis[("Managed Redis\n(In-Memory Cache & Broker)")]:::compNode
            end
        end

        subgraph Cloud_Storage ["3. Object Storage"]
            AWS_S3[("AWS S3 Bucket / Cloudflare R2\n(Multi-modal Image Evidence)")]:::compNode
        end

        Users["全球出海业务用户"] -->|HTTPS (TLS 1.3)| V_Web
        V_BFF -->|mTLS / Secure REST| R_AI
        R_AI <--> R_PG
        R_AI <--> R_Redis
        R_Worker <--> R_Redis
        R_Worker --> R_PG
        R_AI <--> AWS_S3
    end
    class Mode_Prod prodBox;
```
