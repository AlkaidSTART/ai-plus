# 新增需求偏差同步文档与外部访问链接环境变量规则：实施计划

## 目标
在 `AGENTS.md` 中新增一条强制规则，明确以下闭环：
- 当已确认的实际需求或已验证的实现约束与现有文档发生偏差时，以实际需求和已验证实现作为调整依据；文档不是唯一依据，但必须在获批范围内同步修正受影响的文档。
- 运行时访问外部服务的链接（例如亚马逊官网地址）不得硬编码在源码中，必须通过环境变量提供并由项目配置层读取。
- 保留现有“计划 → 明确批准 → 实施 → 验证 → 单独记录结果”的门禁；需求偏差涉及实质性变化时仍须修订计划并重新审批。

## 当前事实及依据
- `AGENTS.md` 第 28-30 行现有规则要求：文档与代码不一致时指出冲突并先确认，不得为了迎合文档虚构实现；尚未明确给出“需求偏差后同步文档”的完整动作闭环。
- `AGENTS.md` 第 32 行标题为 `## Four Principles`，第 34-62 行仅有 4 条原则；全文检索确认没有其他 “Four Principles” 引用，因此新增第 5 条时需要同步把标题改为 `## Five Principles`。
- `AGENTS.md` 第 64-76 行已规定获批后按 `plan.md` 实施并单独生成 `result.md`；本次新增规则需要与该审批门禁保持一致。
- `git ls-files --stage -- AGENTS.md` 显示 `AGENTS.md` 是已跟踪文件；修改后可使用 `git diff` 做精确验证。
- 根目录存在空的环境变量示例文件；本地环境变量覆盖文件的位置也已被 `.gitignore` 覆盖。仓库具备使用环境变量文件的既定位置，因此规则要求示例文件保留非敏感占位值，但不要求本次提前修改它们。
- 用户原文“文档不作为唯一悦色”存在明显输入偏差；本次按最小合理解释为“文档不作为唯一依据”。“访问链接”按最小范围解释为运行时访问外部服务的地址，不包含文档中的资料引用超链接。
- 本次是单文件、精确文本插入任务，没有可独立并行且不重叠的实施或验证工作包，因此不启用子代理。

## 预计变更文件
- `AGENTS.md`：
  - 将 `## Four Principles` 精确改为 `## Five Principles`。
  - 在第 4 条原则之后、`## 任务计划与结果记录（强约束）` 之前新增 `### 5. 需求偏差同步文档，外部访问链接配置化`，写入下述精确内容：
    - 已确认的实际需求和已验证实现是调整依据；文档不是唯一依据；不得为了迎合过时文档而强行实现。
    - 发现偏差先指出；若涉及已批准范围、公开行为、架构、数据/模式、认证授权、依赖、兼容性或部署策略变化，必须先修订 `plan.md` 并重新获批。
    - 获批后在同一任务中同步更新受影响文档，使其反映实际需求和已实现行为；文档同步不得扩大已批准范围。
    - 文档与代码冲突必须明确报告，不得虚构实现迎合文档。
    - 运行时访问外部服务的链接不得硬编码，必须通过环境变量提供并由项目配置层读取；同步维护环境变量示例文件中的变量名和非敏感占位值；不得提交真实密钥或环境专属值；文档资料引用超链接不适用此条。
- `docs/plans/2026-09-14-agents-sync-docs-env-links/plan.md`：本计划及获批后的真实审批记录。
- `docs/plans/2026-09-14-agents-sync-docs-env-links/result.md`：验证完成后新增，记录实际变更、命令、结果和偏差。
- 不修改 `README.md`、`PRD.md`、现有设计/API 文档、代码、配置或环境变量文件；本次只落规则，不排查或迁移现有硬编码链接。

## 实施步骤与分工
1. 用户明确批准本计划后，仅在 `plan.md` 的“审核状态”和必要的修订记录中写入真实批准日期与用户原文。
2. 在 `AGENTS.md` 中把 `## Four Principles` 改为 `## Five Principles`。
3. 在第 4 条原则结束后插入以下精确内容，除 Markdown 换行外不改变其他章节：

````markdown
### 5. 需求偏差同步文档，外部访问链接配置化

当已确认的实际需求或已验证的实现约束与 `README.md`、`PRD.md`、设计/API 文档或其他需求文档不一致时：

- 以已确认的实际需求和已验证的实现作为调整依据；文档不是唯一依据，不得为了迎合过时文档而强行实现。
- 先指出偏差。若偏差改变已批准范围、公开行为、架构、数据/模式、认证授权、依赖、兼容性或部署策略，必须先修订 `plan.md` 并重新获得批准。
- 获批后，在同一任务中同步更新受影响的文档，使其反映实际需求和已实现行为；文档同步本身也必须留在获批范围内。
- 文档与代码冲突时必须明确报告，不得虚构实现来迎合文档。

运行时访问外部服务的链接（例如亚马逊官网地址）不得硬编码在源码中，必须通过环境变量提供并由项目配置层读取。应同步维护环境变量示例文件中的变量名和非敏感占位值；不得提交真实密钥或环境专属值。仅作为文档引用或资料出处的静态超链接不属于运行时访问链接。
````

4. 重新读取 `AGENTS.md` 相关区段，确认新增规则位置、标题编号和正文完整，且没有改动其他章节。
5. 执行验证命令；若发现失败或超出获批范围的修改，停止并如实记录。
6. 验证通过后新增 `result.md`，记录完成状态、实际变更、命令真实输出摘要、计划偏差和遗留问题。
7. 本任务为单文件串行修改，主代理直接实施；不使用子代理，避免制造无意义的并行工作。

## 验证与验收
1. `rg -n '^## Five Principles$|^### 5\\. 需求偏差同步文档，外部访问链接配置化$' AGENTS.md`：同时命中标题和第 5 条原则。
2. `rg -n '文档不是唯一依据|同步更新受影响的文档|不得硬编码|必须通过环境变量|亚马逊官网' AGENTS.md`：命中全部关键约束。
3. `git diff --check -- AGENTS.md`：退出码为 0，无空白错误。
4. `git diff -- AGENTS.md`：仅显示标题从 `Four` 改为 `Five`，以及新增第 5 条原则；不得出现代码、配置、PRD、README 或既有章节的额外改动。
5. `sed -n '28,92p' AGENTS.md` 人工复核上下文：第 5 条位于第 4 条与任务计划章节之间，仍然服从“实质性变化重新审批”，且与第 28-30 行现有文档冲突规则不矛盾。

验收条件：`AGENTS.md` 明确包含需求偏差同步文档和外部访问链接环境变量化的强制规则；其余项目文件不变；真实验证结果全部记录在 `result.md`。

## 风险与非目标
- 风险：“文档不是唯一依据”可能被误读为可以忽略文档。规则同时要求报告冲突、按审批门禁处理并同步文档，因此不取消文档校验或“不得虚构”约束。
- 假设：用户所说的“唯一悦色”指“唯一依据”；“访问链接”指运行时外部服务地址。若意图不同，需要在实施前修订本计划并重新审核。
- 非目标：本次不搜索并修复现有代码中的硬编码 URL，不新增或填写任何环境变量，不修改环境变量示例文件，不改写 PRD/README/设计文档，不执行 commit、push、PR 或部署。

## 审核状态
- 状态：已批准
- 创建日期：2026-09-14
- 批准信息：2026-09-14 用户原文：“开始”
