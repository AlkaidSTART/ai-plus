# AGENTS.md 任务计划与结果工作流调整计划

## 1. 目标

根据用户要求，在根目录 `AGENTS.md` 中清晰落实以下流程：

**只读了解 → 创建 plan.md → 展示并等待用户确认 → 按已批准 plan.md 执行 → 验证 → 单独生成 result.md → 汇报。**

沿用现有规则的最小解释：所有会修改代码、配置、文档、依赖或其他状态的实施任务均适用，不因任务简单而跳过；纯阅读、解释和讨论不强制生成计划与结果，不得借此直接开始修改。

## 2. 当前事实及依据

- 已完整阅读根目录 `PRD.md`（334 行）、`AGENTS.md`（373 行），并核对 `README.md`、`.gitignore` 和 `C:/Users/nextr/AGENTS.md`。
- `PRD.md` 第 1–3、6、8–9 节描述 InsightX 的目标：面向具备改款能力的跨境电商企业，完成 Amazon US 评论采集、bge-m3 痛点聚类、产品本体/包装履约双栏建议、看板及基础证据关联；P1 扩展视觉取证、财务否决与完整溯源，P2 扩展回测、跨平台映射和供应链信号。前后端分离及 Vue/FastAPI/LangGraph/PostgreSQL/pgvector/Redis 均为文档描述的目标设计，不能据此声称功能已实现。
- `AGENTS.md:30–35` 和 `AGENTS.md:53–65` 已要求一任务一目录、先计划、明确批准后实施、范围变化重新审核、单独记录结果及如实记录失败。本次不是从零添加流程，而是提升入口清晰度和可直接使用性，保留现有约束。
- 2026-09-12 的目录检查显示：当前工作区不存在 `frontend/`、`backend/`、`docs/`、`plan/`；`git status --porcelain=v1` 显示 127 个已跟踪文件处于未暂存删除状态，根目录 `PRD.md` 未跟踪。这些均是本任务开始前的状态，不恢复、不覆盖，也不推断删除原因。
- `AGENTS.md:6–19` 仍将前后端目录、启动命令及 `docs/` 下文档写为现状；`AGENTS.md:65` 引用的 `docs/plans/README.md` 当前也不存在。当前需求依据应指向根目录 `PRD.md`；API 契约、技术方案及可运行代码暂无法从现有工作区核验。
- 当前只发现根目录 `AGENTS.md`，未发现适用于本次目标文件的嵌套规则。修改现有 `AGENTS.md`，不创建大小写不同的 `agents.md`。
- 本计划目标目录在创建前不存在，Git 中也没有该目录的已跟踪文件，不覆盖历史任务。

## 3. 预计变更文件

| 文件 | 预计变更 | 时机 |
| --- | --- | --- |
| `docs/plans/2026-09-12-agents-plan-result-workflow/plan.md` | 本计划；批准后仅补充真实审批信息，实质性修订需重新审核 | 当前唯一允许的项目写入 |
| `AGENTS.md` | 强化流程入口、补充最小模板、纠正与当前工作区不符的项目事实和失效引用 | 用户明确批准后 |
| `docs/plans/2026-09-12-agents-plan-result-workflow/result.md` | 记录实施状态、实际变更、实施记录、验证结果、计划偏差和剩余事项 | 实施及验证后；失败或中止也如实记录 |

不修改 `PRD.md`、`README.md`、`.gitignore`、`LICENSE`，不恢复任何已删除文件，不修改全局 AGENTS 文件。

## 4. 实施步骤

1. 展示本计划路径与摘要，停止并等待用户明确批准。当前请求不视为对尚未展示计划的批准，不提前生成 `result.md`。
2. 批准后，在本计划审核状态中记录实际批准日期和用户确认原文；实施前复核工作区，避免覆盖期间产生的新修改。
3. 对 `AGENTS.md` 做局部修改，不整篇重写：
   - 在前部提供醒目的中文流程入口，强调先创建 `plan.md`、展示后等待确认、只按获批计划执行、验证后生成独立 `result.md`。
   - 沿用 `docs/plans/YYYY-MM-DD-<任务简称>/`，计划和结果同目录；保留禁止覆盖历史记录、未经批准禁止实施、实质变化重新审批、失败不得伪装完成等现有规则。
   - 将需求文档定位修正为根目录 `PRD.md`；将前后端技术说明区分为目标设计，不再将缺失目录、无法核验的启动命令及 API/技术方案文件视为现有实现事实。
   - 用本文件内的简短 `plan.md` / `result.md` 模板取代对缺失 `docs/plans/README.md` 的模板依赖。计划模板覆盖目标、事实依据、文件范围、步骤、验证验收、风险/非目标、审核状态；结果模板覆盖完成状态、实际变更、实施记录、验证命令与真实结果、偏差、遗留问题和未执行检查。模板不预填审批或成功结论。
   - 不修改“全局工作流”原文附录；保留纯只读任务例外和项目局部规则的优先级。
4. 执行下述文档验证，并核对改动没有超出范围。
5. 在同目录写入 `result.md`，仅报告实际发生的实施与验证情况；向用户提供结果路径和摘要。

## 5. 分工

本次为单个规则文件的局部调整，当前会话无可直接调用的子代理工具；由主代理完成核对、修改和验证，不为形式化分工引入额外工具、依赖或外部状态。

## 6. 验证与验收

### 检查方式

- `git diff --check -- AGENTS.md docs/plans/2026-09-12-agents-plan-result-workflow/`：检查已跟踪改动的空白错误；新增计划和结果文件另行完整回读检查，不能仅凭此命令声称验证过未跟踪内容。
- `git diff -- AGENTS.md`：逐项核对实际修改与本计划一致，保留现有规则及全局附录原文。
- `git status --short`：核对仅新增当前任务记录及修改 `AGENTS.md`，保留任务开始前的删除与未跟踪状态，不执行提交或暂存。
- 完整回读最终 `AGENTS.md`、本计划和 `result.md`，确认路径、模板、审批门禁、失败记录与只读例外没有冲突，记录内容没有将计划当成完成结果。
- `sha256sum PRD.md README.md .gitignore LICENSE`：与下列只读探索基线比对，确认非目标现存文件未被本任务修改。

| 文件 | 本任务开始时 SHA-256 |
| --- | --- |
| `PRD.md` | `6c902d1548bee452d3e8888c698869063a8cbf24e8a18051e4c7e83786fa0d11` |
| `README.md` | `d0f526540e035710e874e460b6fde1cb9e1eaa30fa806854a75456d7398a40e7` |
| `.gitignore` | `9b46e0734d12f413cb5195415c2760b0739ff7b20378e8aa0338db52da129775` |
| `LICENSE` | `ba68068c4082b843f011b0de8a141be606ac7d3957fe24d49ad53c55ead3aa7b` |

### 验收标准

- 任意实施任务的入口清楚表达：先写 `plan.md`，用户明确确认后按计划执行，再验证并单独写 `result.md`。
- 计划、结果文件位置及最低字段明确；未批准时停止，范围变化重新审核，失败状态如实记录。
- 本次执行本身遵循相同门禁；无事后补造审批、无提前生成成功结果。
- `AGENTS.md` 不再误称当前缺失的代码/文档已存在，需求文档路径正确，全局附录不变。
- 本任务只变更第 3 节列出的文件，既有删除和用户文件保持原状。

不运行前后端构建、单元测试或服务启动：本次只改规则文档，且当前工作区缺少相关代码和依赖清单；不得声称已验证项目运行能力。

## 7. 风险与非目标

- 文档规则只能约束代理执行流程，不能提供程序级强制拦截；本次不新增自动化校验器、Git hook 或 CI。
- PRD 和 README 表达的目标不等于当前实现状态，不据此补造 API、代码、测试结果或完成进度。
- README 中的失效引用不在本次修改范围内；不恢复缺失的模板说明文件，改为在 `AGENTS.md` 内提供最小模板。
- 不调整产品范围、架构选型或业务功能，不安装依赖，不创建提交/分支，不推送或部署。
- 若用户希望连纯阅读和讨论也强制生成 `plan.md` / `result.md`，需修订本计划并重新审核；本次保持现有实施任务边界。

## 8. 审核状态

- 状态：**已批准**。
- 计划创建日期：2026-09-12（来自本机 `date` 命令）。
- 用户原始要求：读取 `D:\Project\ai-plus\PRD.md` 与 `D:\Project\ai-plus\AGENTS.md`，了解项目，并修改 AGENTS 文件，使每次任务先规划 `plan.md`，确认后按计划执行，执行结果生成 `result.md`。
- 用户批准原文：**“开始实施”**（在本计划路径和摘要展示后收到）。
- 批准记录日期：2026-09-12（实施前通过本机 `date` 命令核对日期，不补造消息时间戳）。
- 批准范围：本计划列出的 `AGENTS.md` 局部调整及同目录计划、结果记录；未追加其他范围。
